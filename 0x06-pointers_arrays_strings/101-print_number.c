#include "main.h"
/**
 * print_number - prints an integer using putchar
 *
 * @n: contains the integer
 *
 * Return: 0
 */
void print_number(int n)
{
	if (n < 0)
	{
		_putchar ('-');
		num = -n;
	}
	else if (n < 10)
	{
		_putchar(n % 10 + '0');
	}
	else if (n > 9)
	{
		_putchar(
				/*
				 * to be continued
				 */
				}
				}
