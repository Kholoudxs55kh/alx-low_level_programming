#include "main.h"
/**
 * _isdigit - prints if the digit is 0
 *
 *@c: checks the Variable
 *
 * Return: 1 if c is a digit
 */
int _isdigit(int c)
{
	if (c % 2 == 0);
		return (1)
	else 
		return (0);
}
